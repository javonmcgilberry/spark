/**
 * Global app identity. Single source of truth for the product name
 * used in UI copy, logs, system prompts, Slack-facing strings, and
 * browser page titles. If the app is renamed, change this one value.
 */
export const APP_NAME = 'Spark';
