{
  "name": "spark",
  "version": "0.2.0",
  "private": true,
  "description": "AI onboarding companion for new Webflow engineers. Runs on Webflow Cloud.",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "typecheck": "npm run typecheck --prefix web",
    "test": "npm run test --prefix web",
    "test:watch": "npm run test:watch --prefix web"
  }
}
