/**
 * Shared constants that are compile-time fixed across the app.
 *
 * Only add values here that are genuinely global and never change per
 * environment. Anything configurable belongs in `env.ts`.
 */

export const APP_NAME = 'Spark';
